name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '4.0.0-rc.2'
      SERVERPOD_PASSWORD_database: '8VdCPp1Ae4co_HBBOJmsb9LyxSEu13VY'
      SERVERPOD_PASSWORD_redis: 'Jcg0QlQraTDIFiE5VvoGriXczUrt4GZH'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.4'

      - name: Start containers
        working-directory: server_base_server
        run: docker compose up --quiet-pull --build -d

      - name: Install dependencies
        run: flutter pub get

      - name: Install Serverpod CLI
        run: dart install serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: server_base_server
        run: serverpod generate

      - name: Run tests
        working-directory: server_base_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: server_base_server
        run: docker compose down -v
